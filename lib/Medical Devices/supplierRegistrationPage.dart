import 'package:flutter/material.dart';

class SupplierRegistration extends StatelessWidget {
  const SupplierRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}